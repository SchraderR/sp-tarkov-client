import { ImagePathResolverPipe } from './image-path-resolver.pipe';

describe('ImagePathResolverPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagePathResolverPipe();
    expect(pipe).toBeTruthy();
  });
});
